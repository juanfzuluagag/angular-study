"use strict";
(() => {
    const enviarMision = (xmen) => {
        console.log(`Enviando a ${xmen.nombre} a la misión`);
    };
    const wolwerine = {
        nombre: 'Logan',
        edad: 60
    };
    enviarMision(wolwerine);
})();
