import { HideStringPipe } from './hide-string.pipe';

describe('HideStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HideStringPipe();
    expect(pipe).toBeTruthy();
  });
});
